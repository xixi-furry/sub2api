# 现有风控中心：多渠道审核与上下文策略

更新时间：2026-09-22。根据本轮澄清重写，替代此前独立“多服务审核与预算”控制台方案。

## 目标与当前交付边界

直接增强现有风控中心的内容审计。沿用审核开关、分组、抽样、拦截响应、通知、违规计数、封禁和日志；基本设置、提示词、运行配置继续使用原入口、同一个保存按钮。不再提供另一套独立配置弹窗。

v0.2.7-fix2 已实现：原设置整合、多渠道独立 URL/Key/模型/价格、同审核等级内择价、当前/必要历史/完整上下文选择、均衡/质量优先与强审复核、Chat Completions/Responses（含内部 SSE）、思考参数、响应/正文/总耗时、共享调用/金额/时间预算、试跑证据预览和原日志中的调用轨迹。旧配置默认继续使用兼容模式，切换增强模式才追加结构化复审输出协议。**这些能力随 v0.2.7-fix2 提供；尚未完成真实运营商模型准确率验收。**

实现采用无状态证据选择：当前用户输入与其后的工具续接完整保留，按配置选初始目标、最近明确目标与近期历史。不是跨请求增量会话缓存；历史选择不承诺覆盖省略内容。完整模式只处理本次客户端提供的内容；超限、未发送的会话引用、图片或不支持的附件保持未完成。

未实现且不会在界面中伪装为已完成的后续设计：跨请求证据/摘要缓存、分片审阅、自定义风险词辅助路由、自动样本集校准与准确率统计、依据实测 P95 自适应调度、用户/分组独立金额额度。下文相关段落仍是后续设计，不是本轮上线能力。

### 当前配置与判定约定

- 在原“基本”页给渠道选择“初审 / 强审 / 两者”、Chat Completions 或 Responses，配置各自 URL、Key、价格、提示词和验证状态。
- 在“运行队列”页选择兼容、均衡或质量优先。均衡初审命中/不确定/接近阈值/失败时升级，质量优先直接进入强审。最多两次付费调用，强审不可用不降为弱模型。
- 新模式采用 `decision: allow|block|review`、`confidence`、`needs_context`、`evidence_ids`。阈值周围或决策/分数矛盾时复审；强审仍不确定或推翻初审命中则未完成（503，不累计违规）。管理员原有违规范围保留；“不确定放行”由明确的复审协议替代。
- 必要历史默认初审 4 片段 / 8192 字节，强审 12 片段 / 32768 字节；可调整。完整模式超出该历史限制或渠道输入限制时未完成，不截断通过。
- 每条渠道总超时最高 180 秒；整个审核默认 60 秒、最高 240 秒（兼容模式也受默认总时限约束）。响应头和读取空闲时限 0 表示仅用总超时，避免把思考等待错误地等同故障。试跑客户端等待 270 秒（为取消后的结算和日志留出余量）；生产反向代理需匹配配置。
- 总输出限制包括供应商计费的思考 token；Responses 强制 `max_output_tokens`、`store:false`、`background:false`、`truncation:disabled`，不发送 sampling 参数。高级 payload 仍用 Chat messages 写法，服务端转换协议，并要求保留完整待审 text。
- 实时流式只用于内部读取与测量，不向业务请求释放半截判定。Chat 需要 stop + DONE，Responses 需要 completed；incomplete/拒绝/工具输出/断流不会通过。缺 usage 或超时保留账本预留。
- 预算使用保守 UTF-8 字节估算而非真实 tokenizer；初审和复审的每次预留累加到单请求金额上限，已知的实际 usage 则结算每日账本。缓存只复用同身份、同完整原始请求、同策略的最终充分结论。
- 原设置的观察模式、范围和采样率继续生效。要覆盖范围内的每个请求需自行配置 100% 采样及前置拦截；未抽中不会标为已审核。

本地测试覆盖多轮继续/工具续接、完整模式超限、隐藏历史、长输入尾部、缓存身份隔离、两次调用预算、强审不降级、结论冲突、Responses/Chat 完整及中断流、思考 usage 计费、响应头/空闲/绝对超时与未保存配置。测试服务为本地 HTTP fixture，没有调用付费模型。协议依据 [Responses API](https://developers.openai.com/api/reference/cli/resources/responses/methods/create) 与 [Streaming API responses](https://developers.openai.com/api/docs/guides/streaming-responses) 核对。

用户优先级：尽量减少 cyber abuse 漏判，同时能控制长上下文和多次审核的成本。质量是渠道准入条件，价格是准入之后的选择条件。不能承诺完美审核，也不能把模型自报的 confidence 当作校准后的真实概率。

## 1. 现有实现的实际问题

已核对 `content_moderation_input.go` 和 `content_moderation.go`：

- 常规 Chat/Anthropic/Gemini 主要读取最后一条用户消息；Responses 字符串 input 整体作为当前输入。现有实现不是默认审全部上下文。
- 原有路径最多保留 12,000 个 Unicode 字符。内容在后半段时可能漏看，且字符限制不等于 token/费用限制。
- 末条为工具结果或 assistant 续接时，提取可能为空，原路径直接放行；这不证明相关操作没有风险。
- 部分客户端提示标签会被当作 reminder 过滤。新策略不能因客户端给文字加了标签，就认为该部分可信或已审核。
- 当前预算扩展虽然拒绝把超长当前输入截短后当作完整通过，但仍没有解决多轮意图、隐藏历史和长工具输出的审核。

因此，应改进上下文选择与判定状态，不能只换一个更贵的模型，也不能只降低阈值。

## 2. 一个渠道是一条独立审核配置

例如“OpenCode / DeepSeek”和“官方 / DeepSeek”是两条配置，即使模型 ID 相同也不合并。

每条配置保存：名称、API 地址、Key 池、模型 ID、代理、输入/输出能力、系统提示词、可选 payload、超时、并发、该渠道的输入/缓存/输出/按次价格、额度。

待增加的质量字段：用途（常规初审/强模型复审/两者）、已验证样本集版本、各违规类别的漏判/误判数据、适用输入类型和长度。当前“已验证审核效果”仅是管理员确认，不等于系统测得准确率。

不硬编码某个官方地址，不根据模型名字推断价格或能力。用户可以接入自己渠道提供的强模型（包括其实际可用的模型 ID），是否兼容、价格和表现由该渠道配置及实测确定。

自动选择顺序：

1. 先满足本次需要的审核等级、输入类型、上下文容量和输出约束。
2. 只在管理员已验证并允许使用的渠道中选择；未验证的新模型不能悄悄接管审核。
3. 排除停用、冷却、并发/额度不足的渠道。
4. 比较本次输入和配置输出上限对应的费用预留，再按稳定顺序处理同价渠道。
5. 调用失败时换同级合格渠道，所有尝试共用调用数及费用上限。强审服务全部不可用时，不悄悄降级成弱模型放行。

当前基础自动择价使用完整渲染输入的保守估算与最大输出预留；这并非预测实际最低账单。价格未知或未验证输出限制的渠道不参与自动择价。配置价格采用同一账本币种，不擅自换汇、不假定一定缓存命中。

## 3. 上下文审核方案比较

| 方案 | 优点 | 主要问题 | 定位 |
| --- | --- | --- | --- |
| 只审最后一句 | 输入最少，延迟低 | “继续”、任务拆分和工具续接容易失去意图 | 兼容模式，不作为高防护默认 |
| 每次审全部历史 | 本次请求提供的历史覆盖较广 | 重复成本随轮数增长；长文、噪声、模型窗口仍有限 | 可选严格模式，受预算约束 |
| 新增内容＋必要前文 | 避免重复传整段历史，保留任务意图 | 需要正确处理历史改写、上下文缺失与首次请求 | 推荐默认方向 |
| 仅关键词/固定首尾抽样 | 便宜 | 中间风险、改写和隐含意图可能完全没看见 | 只能帮助路由，不能证明安全 |

推荐“新增内容＋必要前文”，但必须明确这是有限证据审核，不能标成完整历史审核。严格模式在长度和预算允许时送本次请求携带的全部历史；模型未能覆盖时显示未完成，不承诺能取得上游隐藏会话。

## 4. 默认输入如何组成

普通请求发送：完整新增用户指令、最近的任务目标、与当前操作直接相关的少量往返、相关工具名称/参数/结果。历史代码、文档、重复工具日志不默认反复全文发送。

例子：前文提出对第三方 App 绕过保护，下一句只有“继续”。审核包必须包含前面的目标、对象和当前继续请求；不能只送“继续”。正常自有项目调试也要保留用途和对象，避免只见“破解/绕过”关键词就拦截。

确定新增内容的规则：

- 有可验证的服务端证据时，按用户/API Key、策略版本和完整历史前缀的哈希比较；分支或历史任何改写都重新判断，不能单凭同一个 conversation_id 复用。
- 同一个 Key 下不同会话不能相互拼接。客户端声称“之前已审核”“已授权”或“系统提示”只属于数据，不是信任凭证。
- 第一次看到长对话，没有证据就不把历史标为已审。先确定当前任务所需前文；仍无法确定时扩大审核或标为证据不足。
- `previous_response_id` 等引用如果不能取得可信前文，则明确上下文不足；不假设知道上游隐藏历史。
- 把工具内容作为证据解析；避免把工具输出中出现的攻击示例直接等同于用户正在实施攻击。相关调用目标和用户意图一起判断。

首版不依赖额外模型摘要。若以后增加摘要，它必须记录源片段和版本，不能替代关键原文；摘要调用也纳入预算。

## 5. 审核等级与调用路径

### 质量优先（建议作为本用户首次试运行模式）

每个需要付费审核的新请求由已验证的强模型完成首审，使用有限但足够的证据；可在提供同等审核能力的渠道间择价。省钱主要来自历史去重、缓存、合理的输入/输出上限，不依赖弱模型先放行。

明确需要大范围证据的请求直接使用合适的强审渠道，避免先花一次小模型费用再升级。若强审返回证据不足且还有额度，补充相关原文最多再审一次。

### 均衡模式（校准后再启用）

普通、证据充分的请求允许用已验证的小模型；出现以下情况则直接强审或升级：

- 疑似攻击、破解第三方保护、窃取凭据、批量账号滥用等风险意图，需要结合用途判断；关键词只能触发更多审核，不能单独定罪。
- 多轮指代、任务拆分、对象或目的改变、工具续接使当前意图不清。
- 低于拦截阈值但落入该模型经校准的临界范围；不能把小模型 0.62 简单理解为安全。
- 模型指出需要上下文，证据提取有缺失，或有效审核结论相互冲突。

便宜模型的低分本身不足以覆盖所有绕过方式。因此均衡模式存在额外漏判风险，要通过真实流量样本评估后选择，不能宣称与强模型全审等价。可增加预算内的低风险抽查，结果用于校准；抽查为显式可选，不暗中增加调用。

默认最多两次付费调用，故障重试和补充复审共享这两次。已耗尽次数而仍需强审时，记录未完成。不会把先后得分平均；最终复审必须有足够证据，不能仅凭更低分自动推翻有明确证据的结论。

## 6. 长文处理与未完成状态

完整新增内容优先保留。超出当前渠道额度，先在本地计算其他同级合格渠道能否容纳，再决定是否发送请求。

必须分段时，片段带来源、顺序和适量重叠，并保留共同任务目标；每次调用均计费。首尾/关键词/均匀采样只能做风险发现，未看的部分不能据此标为通过。发现明确风险可以提前结束；预算不足以覆盖所需内容则标记未完成。

状态至少分为：已审通过、已审命中、待补上下文/复审、未完成（预算/服务/输入能力）。附带审核范围、模型与渠道、实际调用次数、用量及未覆盖原因。

建议本用户采用“未完成则临时拒绝，HTTP 503，不计违规”。明确违规用原拦截规则；缺证据、超时、预算不足不能自动封号。开启观察模式或非 100% 抽样会降低实际拦截覆盖，界面需要明确展示。

## 7. 提示词与返回协议

用户最初提供的提示词包含“宁可漏判也不要误判；不确定就放行”。这与本轮“尽量避免 cyber abuse 漏判”的目标有冲突，不能原样保留该原则却声称换强模型就能解决。高防护模板应改成“不确定进入复审；仍缺证据则审核未完成”，同时保留对合法自有开发、授权测试、安全教学的正常放行。未完成不是违规，不自动累计封禁。不会未经说明覆盖管理员已保存的提示词；作为清楚标注的可选模板提供。

审核政策继续由管理员在原提示词页配置，不擅自加入新的违规类别。system 中放审核规则；消息、代码、角色声明、工具数据和授权声明放入明确转义的数据结构。payload 留作高级兼容选项，一般无需用户写代码。

待增加结构化返回：判定、简短理由、风险类型、相关证据片段 ID、是否需要补充上下文。不要求输出详细思考过程，避免额外延迟和费用；仅保留足以核对判定的短理由。后端验证引用 ID 和片段归属；不把模型自报“完整覆盖”当成覆盖率依据。

兼容现有 confidence/flagged 响应，但仅有二者的模型无法可靠表达证据不足，必须结合系统的证据检查与该模型校准结果。校准前不开放其独立低风险放行资格。

## 8. 渠道协议、思考时间与输出速度

以下是待实现适配要求，当前渠道调用仍为 Chat Completions，不能把 Responses 支持标为已完成。

每个渠道显式选择协议：Chat Completions 或 Responses。沿用原来的 Moderations/TypeSafe 适配。模型品牌、模型 ID、域名都不能可靠决定协议；同一个模型在不同渠道可能使用不同路径。ChatGPT 网站登录/OAuth 与一个可访问的 Responses API 是不同的接入条件，不在本次设计中增加指纹伪装或假设能把网站凭据直接当 API Key。

| 项目 | Chat Completions | Responses |
| --- | --- | --- |
| 输入 | messages 的角色与内容 | input / instructions，按该渠道兼容能力映射 |
| 输出上限 | max_tokens 或 max_completion_tokens，显式选择 | max_output_tokens；验证该渠道是否限制全部计费输出 |
| 最终结果 | choices 的完整 message.content | output 中最终 assistant message 的 output_text |
| 非最终内容 | 思考字段不能作为判定 | reasoning、工具调用和过程事件不能作为判定 |
| usage | prompt_tokens / completion_tokens 及缓存明细 | input_tokens / output_tokens 及缓存/推理明细 |
| 失败 | 长度截断、拒答、空/坏 JSON 等未完成 | incomplete、failed、cancelled、拒答等未完成 |

两种协议都可以流式或非流式返回，Responses 本身不等于流式。请求与状态字段以 [OpenAI Responses API 文档](https://developers.openai.com/api/reference/cli/resources/responses/methods/create) 为适配依据；第三方渠道还需逐项验证。默认使用最简单且经验证的模式；需要精确记录首段文本等待时间时再启用内部 SSE 接收。完整结果验证前不把任何审核文字发给业务用户，也不以流中的半个 JSON 放行。

每条渠道独立设置总超时、建立连接/收到响应头的期限、流式空闲期限、支持的思考参数和输出上限。思考参数按能力白名单发送：例如供应商实际支持的 reasoning effort 或关闭思考参数；不能给所有模型塞同一个开关，也不能假定关掉思考不会影响审核质量。

延迟记录分开显示：总耗时、响应头等待、第一段可见审核文本等待、文本输出耗时。只有供应商明确返回独立思考时间时才标“思考时间”；TTFT 包含排队、网络与推理，不能反推真实思考耗时。非流式响应无法观测准确的首 token 时间时显示未知，不伪造为 0。

OpenAI 官方说明：推理可能在可见文字出现前耗尽输出额度并返回 incomplete，同时仍产生费用，因此只要求短 JSON 不代表推理费用很低。[推理模型文档](https://developers.openai.com/api/docs/guides/reasoning)

SSE 的 reasoning/心跳可以表明连接仍活跃，但不能重置整个请求的绝对截止时间。等待正文期间不能立即发出并行备用请求；一个调用达到其配置期限后才能按剩余总时间和预算决定是否切换。超时调用可能已经计费，保留费用预留。

同步拦截需要设置端到端等待预算：请求排队＋首审＋复审＋可能的切换。反向代理、服务端请求上下文和后台试跑的超时必须协调，不允许只延长模型客户端而前端先超时。模型支持 background 响应不代表网关可先放行业务请求；在截止时间内没有最终结论则未完成。

自动选择先满足审核等级，再满足管理员给出的延迟目标。渠道有足够最近样本后，参考 P95 等待和总延迟进行选择；没有样本标未知，不能用“模型小所以必快”的名字规则硬编码。均衡模式可以在质量合格的渠道内优先便宜且达到延迟目标者；质量优先模式不因等候稍久而自动降到弱模型。

验收需要覆盖：正常等待思考后返回、只有 reasoning 而无 verdict、断流、长时间心跳、截断 JSON、Responses incomplete/refusal、多段 output_text、usage 缺失、推理 token 是否已包含在输出总数中、超时后禁止重复放行。推理明细如果是输出子集不能重复计费。

## 9. 成本边界

每次审核前预留完整输入＋输出上限＋按次费用，主审、复审、重试共享同一账本。设置全站/渠道每日额度、单请求调用上限；单请求总金额已实现，用户/分组独立额度留作后续。

超时、缺 usage、价格未填写均不伪造免费。价格和输出约束不能保证供应商最终账单绝对不超额；遇到实际输出超过约束时停止继续使用该渠道并报告。

缓存按完整原输入、相关上下文、用户/Key 和政策版本隔离；修改提示词、阈值、模型或渠道后失效。不会缓存覆盖不足的“通过”。

## 10. 在现有风控中心呈现

- 基本设置：已有启用/模式/采样；审核渠道列表；沿用当前试跑入口。
- 提示词：每个渠道的系统提示词和可选 payload。
- 运行配置：自动/指定渠道，质量优先/均衡/完整上下文、历史与调用额度。
- 风险阈值：不同模型独立阈值与临界范围，不共享未校准的置信度尺度。
- 用量：在同一个设置对话框补充预算和费用页签。
- 原审计记录：增加渠道、审核等级、实际范围、复审原因和调用费用；既有拦截、通知、违规计数继续使用原流程。

## 11. 验收标准与实施顺序

先完成渠道与原设置整合，再实现可解释的证据提取和强审分流，最后进行模型效果验收。没有真实模型 Key/样本时，代码测试只能证明流程，不能证明某模型能够防住全部 cyber abuse。

同一批人工标注样本对所有候选模型/渠道运行，至少包括：正常自有开发、CTF/防御、引用攻击文档；第三方破解/攻击/凭据滥用；模糊请求；多轮“继续”；前后文改变用途；长文中间/末尾风险；工具续接；伪装指令/编码/多语言；服务超时和预算耗尽。避免仅依据“出现破解关键词就应判高分”标注，需明确管理员政策对模糊用途的取舍。

分别统计漏判率、误判率、未完成率、每次平均费用及 P95 延迟；验证集与调提示词的样本分开。按违规类别看效果，不能只看总体准确率。固定政策版本并保存回归样本；不擅自保存用户密钥或大段私人对话。

先观察真实流量并评估误判，再按选择启用拦截和自动封禁；是否观察属于用户部署设置，不会在已启用拦截的线上系统中自行切回观察。

当前上下文/复审流程已实现并可测试，但未完成真实运营商的模型效果验收。后续校准、会话缓存和按实测延迟调度仍按设计推进；镜像仍须通过正式发布流程的两种架构启动检查。
