# 本次二开说明

## 基线

2026-09-22 重新基于官方 Wei-Shaw/sub2api 的 main：
`1c0a69c0ceddb2fd21581c17ab09f6c500b89ba1`（2026-09-21）。

原 fork 的旧二开不再继承。当前新增内容包括审核自定义配置、多服务审核与预算控制，以及柔和紫色主题，官方布局、登录流程及其他业务功能沿用上游。

## 配置通用聊天模型审核

1. 管理后台 → 安全审计 → 内容审计 → 内容审计设置。
2. 「基础」中选择 **OpenAI Compatible**，填写审核服务商的 Base URL、模型 ID 和 API Key。
3. 「自定义审核」中选择 **Chat Completions**。
4. 将系统提示词粘贴到「系统提示词」。
5. **payload 留空即可**，无需编写 JavaScript。只有需要自定义请求格式时，才展开「高级：自定义请求（可选）」填写代码；支持 `const wrappedUserContent = ...; const requestBody = ...;` 写法。已填代码可通过「恢复默认请求」清空，系统提示词会保留。
6. 回到「基础」运行审核测试，确认结果后保存。测试使用当前未保存的提示词、代码、模型、Key 和阈值。

系统提示词和可选代码存入现有 settings 配置，无需新增数据库迁移。切换到 TypeSafe AI 后，OpenAI 兼容引擎的配置仍会保留。TypeSafe AI 及其预设模型 `jev-latest` 来自官方基线，使用自己的接口和内置审核规则，不使用自定义系统提示词；它不会随 OpenAI Compatible 请求一起调用。

### payload 变量

| 变量 | 内容 |
| --- | --- |
| `text` | 网关提取的待审核文字 |
| `input` | 原生审核输入，可能是字符串或含 text / image_url 的数组 |
| `config.model` | 基础设置中的模型 ID |
| `config.auditPrompt` | 系统提示词编辑框内容 |
| `isModerationEndpoint` | 选择 Moderations 时为 true，Chat Completions 时为 false |
| `requestBody` | 脚本必须定义的结果，可以是 JSON 字符串或对象 |

脚本只负责构造请求体，后端沿用现有 Key、代理、超时和重试设置发送。不要把 Key 写入脚本。无需 fetch、require 或文件操作；这些 API 不开放。每次运行独立，执行限时 100 ms、栈深度 256，脚本及提示词各限制 64 KiB，请求体限制 16 MiB。脚本仅允许管理员配置，解释器不是进程级内存沙箱。

payload 留空时使用内置请求构造：系统提示词加包裹过的待审内容，temperature 为 0，不开启流式输出。默认构造会转发图片输入；自定义脚本若只使用 text，则不会转发图片。

「填入默认请求示例」提供与默认请求一致的可编辑构造代码，包括图片输入。示例不追加审核政策或输出格式指令，具体规则和输出格式统一由管理员在系统提示词中填写。自定义脚本的 requestBody 必须包含 model，聊天接口还必须包含非空 messages；stream 只能省略或为 false。

### 返回格式

建议系统提示词统一要求只输出以下 JSON 格式，不要在 payload 中追加另一种输出要求：

```json
{"confidence": 0.90, "reason": "命中自定义审核规则"}
```

为兼容已有配置，也支持以下格式：

```json
{"flagged": true, "reason": "命中自定义审核规则"}
```

- confidence 必须是 0–1 的数值，达到「拦截置信度阈值」即命中。默认 0.85，可自行调整。
- 只有 flagged 时直接使用布尔值；两者同时返回时以 confidence 为准。新试跑结果通过 `engine_meta.decision_source` 区分两者：布尔结果不显示为 0%/100% 置信度；confidence 结果显示本次分数与阈值的判定说明。旧记录未保存该字段时，保留原有展示。
- 聊天模型结果记为 custom 类别；原来的分类阈值继续用于 Moderations / TypeSafe AI。
- reason 会显示在测试结果和审核记录详情，并进行现有敏感信息脱敏。
- 无效 JSON、空结果、截断、拒答、脚本错误或超时均视为审核异常，不冒充“审核通过”。是否继续转发沿用上游既有的失败放行策略。
- 原生 Moderations 和 TypeSafe AI 的审核逻辑仍可使用。

聊天接口 Base URL 支持主机根地址（自动补 /v1/chat/completions）、以 /v1 或自定义路径结尾的基地址，以及完整 /chat/completions 地址。不要在 URL 中填写 Key。

请按实际试跑延迟设置 HTTP 超时并预留余量；聊天模型可能超过原有 3000 ms 默认值，超时仍沿用失败放行。

原有的抽样比例、提取最新输入、输入长度限制、观察 / 前置拦截、关键词、缓存、通知及封禁策略仍然适用。提示词不是审核效果保证，需要用实际业务样本校准。

## 上下文增强、分级复审与 Responses（v0.2.7-fix2）

入口保持为风控中心 → **内容审计设置**。以下增强随 `v0.2.7-fix2` 提供。版本镜像经两种架构的启动检查后发布，再更新 `latest`。升级后保留现有设置，通用模型渠道及增强模式需要在原设置中启用。

1. “基础”页按运营商添加渠道，各自配置 URL、Key、模型、价格和验证状态。可选择初审/强审/两者及 Chat Completions / Responses；思考、内部流式、响应头和读取空闲超时在高级项中。不同渠道即使模型名一样也独立选择和计费。
2. “自定义审核”页仍是各渠道的系统提示词和可选 payload。payload 留空可用；增强模式追加 `allow/block/review` 判定协议与证据编号，替代“不确定放行”的旧输出行为，违规类别仍由原提示词决定。
3. “运行队列”页选择兼容、均衡或质量优先，及当前/必要历史/完整上下文。均衡在命中、临界、不确定或服务失败时升级；质量优先直接强审，强审失败不会降到初审渠道。自动择价仅在符合本次审核等级的渠道内进行。
4. 当前请求和后续工具内容不截断；必要历史保留初始目标和最近明确目标，再补近期片段。完整模式只审客户端实际提供的全部内容；隐藏会话引用、附件、不支持的输入或预算不足会未完成。不是跨轮会话摘要缓存。
5. 初审、强审与切换共用最多两次付费调用、单请求金额及时间预算。复审仍不确定或与初审命中冲突时未完成；增强模式要求前置拦截中的未完成处置为 503，不累计违规。观察/范围/采样继续由原设置控制。
6. “基础”试跑可输入文字或 Chat/Responses/Anthropic/Gemini 请求 JSON。“仅预估”展示实际选择范围与证据摘要，不调用模型；计费试跑使用已保存配置和共享账本。调用轨迹展示所用渠道、审核阶段、响应等待、首段可见正文和总耗时，原审计记录详情也可查看。

完整输出才可采用：Chat SSE 要求 stop + DONE，Responses 要求 completed；reasoning、incomplete、拒绝、断流或半截 JSON 不通过。推理 token 属于输出用量子集时不重复计费。具体参数兼容范围仍需在实际渠道试跑。未知费用和超时仍保留预算预留。

本地 HTTP fixture、Go 回归、前端组件/类型/构建和浏览器桌面/深色/手机布局检查用于验证程序流程；不代表真实模型已达到某种漏判率。后续会话证据缓存、自动校准、按实测 P95 调度和分组额度仍未实现。详细配置与当前边界见 [MODERATION_COST_DESIGN.md](MODERATION_COST_DESIGN.md)。

## 多服务审核与预算（第一阶段与兼容模式）

入口：现有风控中心 → **内容审计设置**，渠道在基础页、提示词在自定义审核页、策略在运行队列页、费用在预算与用量页。这些设置从 `v0.2.7-fix2` 起提供；`v0.2.7-fix1` 不包含它们。

- 新配置默认关闭；旧 Chat Completions 服务可派生为第一家服务，原 Key 在服务端保留。原有引擎配置不被覆盖，关闭新版后继续使用旧引擎。
- 每家独立 URL、模型、Key 池、提示词、可选 payload、阈值、代理、价格和限额。Key 输入留空表示保留；填写后替换该服务的整个 Key 池；清空有单独选项。保存使用 revision 防止覆盖另一页面的修改。
- 最多配置 20 家服务，每条路由选择一个主服务和一个备用。单请求最多两次模型调用，备用也占用总额度。400/401/403 等配置错误本次不继续重试；429/5xx 可调用备用，后续请求遵守冷却。服务并发或服务日额度不足可使用备用，全站额度不足停止调用。
- 保留总开关、模式、分组、模型筛选、抽样率、关键词、通知及自动封禁。新版不使用旧版全局命中哈希缓存。当前用量与 Key 在原内容审计设置中查看；通用模型渠道启用时隐藏不适用的旧 Key 统计与命中哈希设置。
- **本阶段只审核当前用户文字**；不自动发送全历史，不做分段或语义复审。图片、其他附件、工具续接、超长输入、无效判定和服务故障都标记为未完成审核。管理员必须选择临时拒绝（503，`content_moderation_unavailable`，不计违规）或允许并记录；观察模式始终放行。未抽样的请求允许并记录未审核。
- 输入上限以完整序列化请求的 UTF-8 字节数加 256 估算，包含系统提示词及 payload。这是保守预算，**不是精确 token 数**，中文长提示词可能需要提高默认 4096；先用“仅预估”确认。不会截断长输入然后声称全部通过。输出强制使用所选 `max_tokens` 或 `max_completion_tokens`、`n=1`、非流式；payload 不得改模型或增加工具/图片等未计量参数。
- 金额使用十进制定点计算，币种首次保存后固定。所有运营商价格使用同一币种，暂不自动汇率换算。金额留空不限、填 0 停止调用；次数和 token 日额度填 0 表示不限。设置金额预算前需要价格和输出限制已验证声明。
- PostgreSQL 事务按全站/服务固定顺序锁定日账本，调用前预留输入+最大输出费用，返回后按 usage 结算。未知价格、超时、缺失/异常 usage 保留预留并标为待确认；不会把未知费用显示成真实免费调用。服务实际 token 超出估算/输出限制后暂停当天新调用。运营商不遵守输出限制或自行计费时无法承诺账单绝对封顶。
- 账本每日 UTC 重置，跨日结算仍写入原日。正常/命中判定都可缓存；缓存包含认证用户、API Key、完整原请求及全部策略配置的哈希，只保存判定，不保存待审正文；配置版本、阈值或上下文变化会失效。输入不完整或判定失败不入缓存。独立服务端事件 ID 避免客户端关联 ID 被用于绕过去重。
- **新版试跑使用已保存配置**，不使用草稿；共享线上预算，可能使用备用，但不写判定缓存、不计违规、不封号。未保存时禁用计费试跑。启用新版后旧试跑接口关闭，避免绕过预算。
- 新增迁移 `240_fork_content_moderation_v2.sql`，只新建 `fork_moderation_*` 表。缓存过期清理，事件及尝试明细保留 90 天；日汇总账本保留。没有更改官方历史迁移。尚不支持管理员重算旧未知账单，未知预留在所属日继续占用。

2026-09-22 方向修正：渠道编辑已整合到原风控中心基本设置，提示词、运行策略及用量均在原设置对话框内，共用原保存按钮；取消独立多服务弹窗。原设置与渠道配置在同一 PostgreSQL 事务中保存，渠道 revision 冲突时全部保持不变。新增按各渠道真实配置价格自动择价，只有管理员确认审核效果、填写价格并确认输出限制的启用渠道参与；同模型不同地址分别定价。未知价格不当作免费，故障和额度限制切换仍共享调用上限。

v0.2.7-fix2 已增加上文描述的上下文、复审及协议能力；本节的末条文字/非流式说明仅描述第一阶段与兼容配置。当前 API `/admin/risk-control/v2/{config,usage,preview,test}`；preview/test 可提交 text，或 protocol + body。保存统一由原设置接口原子完成；配置示例和原型属于设计资料，以实际管理 API 为准。

## 主题

通过 Tailwind 共享颜色调整主色、灰阶、深色背景、阴影和登录背景，保留官方布局和功能。使用原有系统字体栈，没有增加字体文件。

## 构建与上线

仍使用官方项目的构建方式，前端使用 **pnpm 9**、后端使用 **Go 1.27.0**。例如在仓库根目录构建本地镜像：

```sh
docker build -t sub2api:custom .
```

部署时将原服务的 image 改为自己构建的镜像。继续使用官方预构建镜像不会包含这些修改。已发布本仓库的二开镜像；尚未替换你的线上容器，也未改动线上数据库。

## 验证

- Go 内容审核、配置、代理、引擎及管理接口相关测试。
- 新增脚本执行 / 超时、返回解析、配置持久化、引擎切换和网关拦截测试。
- 前端 lint、TypeScript 类型检查、生产构建。
- Linux amd64 后端编译通过（包含前端静态资源）。
- 本次设置改进通过审核页面、请求示例与翻译测试 23 项；fix1 发行前已通过 Makefile 指定的前端关键测试 274 项。
- 1440 px 桌面浅色 / 深色、390 px 手机浏览器检查，无页面异常或编辑器横向溢出。
- 视觉预览使用本地模拟数据；未使用真实审核模型 Key，因此未验证具体服务商的审核质量或延迟。

新增 JavaScript 解释器采用 [Goja](https://github.com/dop251/goja)，版本及依赖校验保存在 go.mod / go.sum。


## 二开发行与官方同步

镜像仓库：`ghcr.io/xixi-furry/sub2api`。正式二开版本采用 `v官方版本-fixN`：
`v0.2.7-fix1` → `v0.2.7-fix2`；更新官方到 `v0.2.8` 后，从 `v0.2.8-fix1` 开始。

每个版本提供带 v 和不带 v 的镜像标签（例如 `v0.2.7-fix1` 和 `0.2.7-fix1`），以及成功发布后更新的 `latest`。部署建议固定版本标签；支持 linux/amd64 和 linux/arm64。已发布的版本号不重复使用。

### 发布新功能

1. 把修改提交到 main。
2. GitHub → Actions → **Fork Release** → Run workflow。
3. ref 保持 main，tag 留空即可自动计算下一个 fix 编号，也可填写与当前官方基线一致的明确版本。
4. 完整 CI 通过后构建前端、两种架构的二进制与镜像，启动临时 PostgreSQL / Redis 验证初始化、HTTP 健康检查和前端页面。
5. 两种架构都通过后才发布版本镜像、GitHub Release、Linux 升级包及校验文件，最后更新 latest。

版本号通过构建参数注入；保留官方 VERSION 文件，减少以后合并冲突。二开镜像的后台更新和回滚源设为本仓库，并支持 fix 编号数值比较（fix10 大于 fix9）；不会复用官方镜像遗留的升级缓存。普通源码构建仍沿用默认更新源；本仓库正式镜像由 Fork Release 工作流生成。

### 自动同步官方

**Sync Official Releases** 按北京时间每天约 05:23 检查官方最新正式 Release，也可在 Actions 手动运行。GitHub 的计划任务可能延迟执行；公开仓库连续 60 天无活动时，GitHub 可能停用定时任务，届时需在 Actions 重新启用。

- 跟随正式版，不自动跟随每日 main 提交或预发布版。
- 创建候选分支，以普通 Git merge 合并，保留二开提交历史；不做强制同步或整树替换。
- 有文本冲突时中止并列出冲突文件，不自动选择“全部官方”或“全部本地”。
- 无冲突时运行完整 CI，包括自定义审核页面、后端审核、版本规则和合并保护测试。
- 测试失败时保留候选分支供排查，main 和已发布镜像不变。
- 测试通过且 main 未被其他人更新，才快进 main，并触发新版本发布。
- 同步和发布只更新 GitHub 代码与镜像，不自动更换服务器上的容器。

自动合并不能保证所有未来改动在业务上都无损；回归测试能降低风险，有冲突或检查失败仍需人工适配。不要使用 GitHub 的“丢弃提交并同步 fork”，否则可能抹掉二开。

上游基线记录在 `.github/fork.json`，同步工作流负责更新它。后续增加二开功能时，应一并补充回归测试。

## 历史发布：v0.2.7-fix1

- 版本：v0.2.7-fix1（2026-09-22）。
- 镜像：`ghcr.io/xixi-furry/sub2api:v0.2.7-fix1`，另提供不带 v 的固定标签；`latest` 已随之后的正式发布更新。
- 多架构摘要：`sha256:104896c19784918895ec9fa29b1f2998226fe41051a5eba9f8c283e103997c8c`。
- amd64 / arm64 镜像均通过数据库初始化、HTTP 健康检查和前端页面检查，并已验证匿名访问及源码标识。
- [发布页](https://github.com/xixi-furry/sub2api/releases/tag/v0.2.7-fix1)；[完整构建及验证记录](https://github.com/xixi-furry/sub2api/actions/runs/35684041796)。
- 自动同步工作流已启用，首次无更新检查通过。服务器需另行切换镜像；更换前备份数据库，保留数据卷和环境配置。


## 当前发布：v0.2.7-fix2

- 发布时间：2026-09-22 17:38（北京时间）；正式版，非预发布。
- 发布源码：`f2196f358d337bc3d7ef2366d1100db3f59c1519`；后续文档提交不改变此版本源码。
- `v0.2.7-fix2`、`0.2.7-fix2` 和 `latest` 已核对为同一多架构摘要：`sha256:2ae5dacfa9b866919d011dee7bd293db326b3e25eb3d86774a331b5ada9f7456`。
- 完整 CI 成功；linux/amd64、linux/arm64 均通过版本、数据库初始化、健康接口和内嵌前端检查。发布后匿名读取镜像清单及配置成功，校验了摘要、两种架构、版本与源码标识。
- [正式发布页](https://github.com/xixi-furry/sub2api/releases/tag/v0.2.7-fix2)；[完整构建及验证记录](https://github.com/xixi-furry/sub2api/actions/runs/35710039639)。发布附件含两种架构升级包及 `checksums.txt`。
- 尚未操作服务器上的容器。真实运营商效果、费用与耗时需部署后结合试跑和审计日志验证。

### 部署与启用

镜像：`ghcr.io/xixi-furry/sub2api:v0.2.7-fix2`，同时提供 `0.2.7-fix2`；`latest` 已指向本版本。这次发行包含原风控中心的多渠道审核、上下文策略、强审、Chat Completions / Responses 和共享预算，不调整业务模型的 OAuth 指纹。

升级容器时保留原 PostgreSQL、Redis、数据卷和环境变量。首次启动自动执行 `240_fork_content_moderation_v2.sql`，新建独立 `fork_moderation_*` 表；不会清空现有数据。更新前保存数据库备份。旧引擎和审核策略继续保留，启用新模式前确认渠道、价格、总预算与未完成时的处置。

- “基础”：勾选使用通用模型审核渠道；填写各运营商 URL、模型、Key、协议、初审/强审用途及费用。Key 在服务端保存，输入留空保留已有 Key。
- “自定义审核”：填写每个渠道的系统提示词，payload 通常留空。增强模式自动追加证据编号与 allow/block/review 输出协议。
- “运行队列”：选择均衡或质量优先、必要历史或完整上下文、总调用/时间/金额上限。质量优先只用已验证的强审渠道；未完成时可暂时拒绝，且不会记为违规。采样和观察模式仍由原设置控制。
- “预算与用量”及审计记录：查看调用、确认费用、未结算预留、审核阶段、实际覆盖范围和耗时。要记录未命中请求，需要启用记录未命中输入。

真实审核模型的准确率和耗时尚未用运营商 Key 验证；后续可在服务器后台试跑并结合记录校准。计费试跑使用已保存配置，计入总预算。使用自动更新器的部署可跟随 `latest`；Docker 本身不会因为有新 Release 就自动重建现有容器。

## 设置简化（本地待发布，2026-09-23）

- 保持原风控中心和原保存接口。新版渠道开启时，常用入口缩减为“审核配置 / 试跑 / 预算”；原有范围、队列、响应、关键词和保留策略可通过“更多设置”访问。
- 将审核策略、模型地址、Key 与系统提示词集中到审核配置；接口协议、思考参数、超时、限额与可选 payload 放入折叠的高级设置。自动择价时价格面板默认展开，避免遗漏必需价格和输出限制确认。
- 明确区分原审核引擎、通用渠道的兼容模式和增强模式。页面展示已保存的审核模式；编辑草稿不会提前改变线上状态。选择均衡或质量优先时，在草稿中同步选择后端要求的“未完成暂时拒绝”。
- 未修改后端审核分流或自动迁移配置；升级本身不启用增强模式。旧审核引擎本来会调用模型，但不会自动使用新版上下文策略和分级复审。
- 前端 ESLint、类型检查、生产构建、37 项设置、payload 及翻译回归通过；本地模拟预览通过 1440 px 浅色/深色与 390 px 手机检查，无横向溢出或页面异常，切换入口保留草稿、Key 和高级参数。
- 配置有未保存修改时，预估和计费试跑均提示先保存。可直接“保存配置并留在此页”，保留测试输入；保存失败不启用试跑，保存本身不调用模型。后端默认字段先统一规范化，避免刚打开设置就误报已修改；配置变化后不展示旧请求的判定。
- 这轮变更尚未发布镜像；已发布的 v0.2.7-fix2 / latest 不受本地预览影响。预览不调用真实模型，不连接线上数据库。

## 模型价格目录（本地待发布，2026-09-23）

在“审核配置 → 渠道 → 价格与费用控制”点击“从 models.dev 查价”，按模型 ID 或名称搜索。优先匹配当前 API 地址对应的运营商，并展示同名模型的其他运营商价格；OpenCode Zen 和 Go 按路径区分。未知中转商或模型别名不自动当作官方渠道，使用其他条目作为参考需要在页面确认。

- 数据来源：<https://models.dev/api.json>，公开文档定义单价为 USD / 百万 token。导入输入、输出及缓存读取价格到草稿；原每请求附加费保留。保存前不影响线上审核。
- USD 账本直接填价；CNY 账本填写实际结算汇率后换算。金额按十位小数向上取整，避免浮点误差低估预留；不会修改已建立账本的币种。未知价格不作零价，特殊阶梯价、缓存写入价或不同推理单价只展示参考，不能一键把基础价当完整费用。
- 价格不代表审核能力。导入不改变模型、URL、Key、初审/强审用途、审核已验证标记或输出限制已验证标记；现有审核等级过滤后再择价的逻辑保持不变。
- 管理员 API：`GET /admin/risk-control/v2/model-prices?q=...&base_url=...`，仅请求固定 models.dev 地址，不向外传递渠道 URL、搜索词、Key、提示词或用户内容。服务端缓存 6 小时，失败后短暂退避；不超过 7 天的旧数据可以返回并明确标记过期，首次失败或缓存过旧则提示手动填写。下载限时 10 秒、响应上限 16 MiB，不跟随重定向。
- 只有管理员查价时加载公开目录，审核热路径不依赖目录。再次查价不会自动覆盖已保存价格；要更新实际价格仍需导入并保存。当前不自动查询汇率或推断模型审核能力。
- 新增后端真实 API 读取检查已通过（`MODELS_DEV_LIVE=1` 时运行）；普通回归使用固定 fixture，涵盖渠道隔离、并发缓存、断网旧缓存、缺价/零价、异常响应、特殊价格。原审核及模型目录相关测试、管理接口和路由编译通过。
- 前端 ESLint、类型检查、生产构建、42 项组件/金额/翻译回归通过；1440 px 浅色/深色与 390 px 手机实测导入，保留附加费且不提升审核资格。新回归已加入 Fork CI。
- 本地预览使用当天公开价格快照与模拟管理 API，不连接线上数据库、不调用审核模型。此变更尚未发布 Release 或镜像。
